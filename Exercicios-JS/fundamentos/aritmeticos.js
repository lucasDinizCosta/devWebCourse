const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d 
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

// NO ES7 tem a versao de exponeciação mais facil
const potencia = a ** b

console.log(soma, subtracao, multiplicacao, divisao, modulo, potencia);
// Operadores binarios pois trabalham com dois operandos

// Operadores unarios são aqueles que trabalham com um operando só, tipo: -a